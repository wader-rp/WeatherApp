import React from 'react';
import {StyledHeader} from './StyledHeader'

const Header = () => {
    return (
        <StyledHeader>
            alasgas
        </StyledHeader>
    );
};

export default Header;